server {
    listen 9000 ssl;
    ssl_certificate     /etc/nginx/certs/public.crt;
    ssl_certificate_key /etc/nginx/certs/private.key;
    location / {
        proxy_pass http://minio1:9000;
        proxy_set_header Host minio1:9000;
    }
}
