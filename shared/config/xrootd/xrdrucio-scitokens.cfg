# Ref: https://xrootd-howto.readthedocs.io/en/latest/tpc/
# Data exposure
all.export /data
xrd.port 1094

# TLS (required for tokens)
xrd.tls /etc/grid-security/xrd/xrdcert.pem /etc/grid-security/xrd/xrdkey.pem
xrd.tlsca certdir /etc/grid-security/certificates
xrootd.tls capable all

# HTTP + TPC
xrd.protocol http:1094 libXrdHttp.so
http.exthandler xrdtpc libXrdHttpTPC.so
http.header2cgi Authorization authz

# Map filesystem to HTTP
http.staticpath /data
http.listing /data

# Authorization (SciTokens only)
ofs.authorize 1
ofs.authlib ++ libXrdAccSciTokens.so config=/etc/xrootd/scitokens.conf
acc.authdb /etc/xrootd/authdb

# Optional but useful for debugging
scitokens.trace all
