[common]
logdir = /var/log/rucio
loglevel = INFO

[database]
default = postgresql+psycopg://rucio:rucio@ruciodb-oidc/rucio
pool_recycle = 3600
echo = 0
pool_reset_on_return = rollback

[policy]
permission = generic
schema = generic
lfn2pfn_algorithm_default = hash
support = https://github.com/rucio/rucio/issues/
support_rucio = https://github.com/rucio/rucio/issues/

[client]
rucio_host = http://rucio-oidc
auth_host = http://rucio-oidc
auth_type = userpass
username = ddmlab
password = secret
account = root
request_retries = 3

[bootstrap]
userpass_identity = ddmlab
userpass_pwd = secret
userpass_email = rucio-dev@cern.ch

[oidc]
idpsecrets = /opt/rucio/etc/idpsecrets.json
issuer = https://keycloak:8443/realms/rucio
expected_audience = rucio-oidc
admin_issuer = rucio-oidc

[trace]
tracedir = /var/log/rucio/trace
brokers = localhost
port = 61013
topic = /topic/rucio.tracer
username = rucio
password = rucio

[conveyor]
scheme = root,davs,s3,https,http
transfertool = fts3
cacert = /etc/grid-security/certificates/rucio_ca.pem
usercert = /etc/grid-security/hostcert_with_key.pem
use_certificate_proxy = False
allow_user_oidc_tokens = True
default_lifetime = -1
poller_oidc_support = True

[alembic]
cfg = /opt/rucio/etc/alembic.ini

[api]
endpoints = accountlimits, accounts, archives, auth, config, credentials, dids, export, heartbeats, identities, import, lifetime_exceptions, locks, meta_conventions, ping, redirect, replicas, requests, rses, rules, scopes, subscriptions, vos
